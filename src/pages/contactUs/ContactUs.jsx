import React from 'react';
import Error from '../../components/error/Error';

const ContactUs = () => {
    return (
        <div>
            <Error/>
        </div>
    );
};

export default ContactUs;